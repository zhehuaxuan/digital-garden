{
  "buildCommand": "mkdir -p .quartz-cache && npm run install-plugins && npm run quartz build",
  "outputDirectory": "public",
  "installCommand": "npm install",
  "framework": null,
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
